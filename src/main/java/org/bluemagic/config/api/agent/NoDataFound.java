package org.bluemagic.config.api.agent;

/**
 * Marker object pattern. Use this object in cases where no data was found
 * this allows us to differentiate between a null value and no return value
 * (we have a NullObject class too).
 **/
public class NoDataFound {
}