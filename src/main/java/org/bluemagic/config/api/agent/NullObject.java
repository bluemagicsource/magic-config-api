package org.bluemagic.config.api.agent;

/**
 * Marker object pattern. Use this object in cases where we want to explicitly
 * return a null value this allows us to differentiate between a null value and
 * no data found (we have a NoDataFound class too).
 **/
public class NullObject {
}